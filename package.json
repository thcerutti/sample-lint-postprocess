{
  "name": "my-lint",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src/**/*.js",
    "full-lint": "rm -rf node_modules && yarn && POST_URL=abc123 && yarn lint"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.21.0",
    "eslint-plugin-my-lint": "file:packages/eslint"
  }
}
